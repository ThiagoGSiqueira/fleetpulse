spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fleetpulse_db
    username: postgres
    password: postgrespassword

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
